{% extends 'base.html.twig' %}

{% block title %}Rarete index{% endblock %}

{% block body %}
    <h1>Rarete index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_rarete</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rarete in raretes %}
            <tr>
                <td>{{ rarete.id }}</td>
                <td>{{ rarete.nomRarete }}</td>
                <td>
                    <a href="{{ path('app_rarete_show', {'id': rarete.id}) }}">show</a>
                    <a href="{{ path('app_rarete_edit', {'id': rarete.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_rarete_new') }}">Create new</a>
{% endblock %}
