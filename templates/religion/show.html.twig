{% extends 'base.html.twig' %}

{% block title %}Religion{% endblock %}

{% block body %}
    <h1>Religion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ religion.id }}</td>
            </tr>
            <tr>
                <th>Nom_religion</th>
                <td>{{ religion.nomReligion }}</td>
            </tr>
            <tr>
                <th>Description_religion</th>
                <td>{{ religion.descriptionReligion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_religion_index') }}">back to list</a>

    <a href="{{ path('app_religion_edit', {'id': religion.id}) }}">edit</a>

    {{ include('religion/_delete_form.html.twig') }}
{% endblock %}
