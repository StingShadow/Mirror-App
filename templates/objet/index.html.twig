{% extends 'base.html.twig' %}

{% block title %}Objet index{% endblock %}

{% block body %}
    <h1>Objet index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_objet</th>
                <th>Description_objet</th>
                <th>Effet_objet</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for objet in objets %}
            <tr>
                <td>{{ objet.id }}</td>
                <td>{{ objet.nomObjet }}</td>
                <td>{{ objet.descriptionObjet }}</td>
                <td>{{ objet.effetObjet }}</td>
                <td>
                    <a href="{{ path('app_objet_show', {'id': objet.id}) }}">show</a>
                    <a href="{{ path('app_objet_edit', {'id': objet.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_objet_new') }}">Create new</a>
{% endblock %}
