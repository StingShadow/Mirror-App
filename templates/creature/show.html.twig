{% extends 'base.html.twig' %}

{% block title %}Creature{% endblock %}

{% block body %}
    <h1>Creature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ creature.id }}</td>
            </tr>
            <tr>
                <th>Nom_creature</th>
                <td>{{ creature.nomCreature }}</td>
            </tr>
            <tr>
                <th>Description_creature</th>
                <td>{{ creature.descriptionCreature }}</td>
            </tr>
            <tr>
                <th>Habitat_creature</th>
                <td>{{ creature.habitatCreature }}</td>
            </tr>
            <tr>
                <th>Regime_creature</th>
                <td>{{ creature.regimeCreature }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_creature_index') }}">back to list</a>

    <a href="{{ path('app_creature_edit', {'id': creature.id}) }}">edit</a>

    {{ include('creature/_delete_form.html.twig') }}
{% endblock %}
