<form method="post" action="{{ path('app_source_pouvoir_delete', {'id': source_pouvoir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ source_pouvoir.id) }}">
    <button class="btn">Delete</button>
</form>
